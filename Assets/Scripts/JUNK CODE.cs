using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JUNKCODE
{

    /*
        void setMinAndMax()
        {
            //Vector3 bounds = Camera.main.ScreenToWorldPoint(new Vector3(Screen.width, Screen.height, 0));
           // Debug.Log("bounds = " + maxX);
            //ebug.Log("- bounds = " + minX);

        }//setMinMax

        //****************************************************************
        // WriteHighScoreToFile                                         
        // Create a new file to hold the highscore so that it can be    
        // repulled and used after game is closed                       
        //****************************************************************
        public void WriteHighScoreToFile(int highscore)
        {
            string path = "highscore.txt";

            File.WriteAllText(path, highscore.ToString());

        }

        //****************************************************************
        // ReadHighScoreFromFile()                                      
        // Reads in the last highscore from file so current game        
        // results can be compared against it                           
        //****************************************************************
        public void ReadHighScoreFromFile()
        {
            if (File.Exists("highscore.txt"))
            {
                string path = "highscore.txt";
                //Read the text from directly from the test.txt file
                StreamReader reader = new StreamReader(path);
                highScore = int.Parse(reader.ReadToEnd());

                reader.Close();
            }
            else
            {
                highScore = 0;
            }
        }



        public void AddToHighScoreList(int highscore)
        {
            highScoreList.Add(highscore);
            highScoreList.Sort();
        }

        public List<int> GetHighScoreList()
        {
            return highScoreList;
        }

        public void UpdateHighScoreList(List<int> newHighScoreList)
        {
            highScoreList = newHighScoreList;
        }

        */
}